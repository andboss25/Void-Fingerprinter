# Void-Fingerprinter
Void-Fingerprinter is a web vulnerability testing application that detects what technologies are used in a project, so it can compile a list of possible vulnerable points and paths to test.
